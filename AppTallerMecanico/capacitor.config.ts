import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.app_servi_express.app',
  appName: 'com.app_servi_express.app',
  webDir: 'www'
};

export default config;
